package  {	import flash.geom.Point;		public class Checkpoint event {		var lastestCheckpoint:Point;				public function Checkpoint() {						// constructor code			this.addEventListener(Event.ADDED_TO_STAGE, onAddedToStage);		}		public function onAddedToStage(event:Event)		{			//remove the intializer			this.removeEventListener(Event.ADDED_TO_STAGE, onAddedToStage);			this.addEventListener(Event.ENTER_FRAME, onTick);		}//end function				public function onTick(event:Event){							if(this.hitTestObject(Game.gameContainer.hero) && )				{					latestCheckpoint = new Point(Game.gameContainer.levelMap.x, Game.gameContainer.levelMap.y);				}		}//end method				public function callCheckpoint(){			Game.gameContainer.levelMap.x = latestCheckpoint.x;			Game.gameContainer.levelMap.y = latestCheckpoint.y;		}	}	}